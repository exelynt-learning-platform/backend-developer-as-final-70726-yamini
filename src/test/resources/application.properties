# Test properties: provide a test JWT secret for unit/integration tests
# This must be >= 32 bytes (256 bits). Use a secure value in CI secrets.
app.jwt.secret=test-jwt-secret-that-is-long-enough-for-tests-00001
app.jwt.expiration=3600000
app.seed.enabled=true
