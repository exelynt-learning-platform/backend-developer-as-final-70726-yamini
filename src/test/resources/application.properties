# Test properties: provide a test JWT secret for unit/integration tests
# We use a randomized 64-character (512-bit) hex string at test-time to avoid hardcoded secrets.
app.jwt.secret=${random.uuid}${random.uuid}
app.jwt.expiration=3600000
app.seed.enabled=true
